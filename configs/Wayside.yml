# ------------------- General Options -------------------------
    description                  : AB3DMOT
    seed                         : 0
    
    # --------------- main.py
    save_root                    : ./results/Wayside
    dataset                      : Wayside      # KITTI
    split                        : val        # val, test
    det_name                     : mark  # name of the detector [pointrcnn, pvrcnn]
    cat_list                     : ['Car'] # upper case first letter
    
    score_threshold              : -10000     # filter out tracklet with low confidence if necessary, default no filtering here but do it in trk_conf_threshold.py
    num_hypo                     : 1          # >1 to allow multi-hypothesis tracking
    
    # --------------- model.py
    ego_com                      : false      # turn on only slightly reduce speed but increase a lot for performance
    vis                          : false       # only for debug or visualization purpose, will significantly reduce speed
    affi_pro                     : false
    #---------------- addition
    label_format                 : Wayside      # Kitti, Waymo, nuscenes
    pcd_db_root                  : ./data/Wayside/pcd_db/mark_dense/
    class_map                    : {"truck": "car"} #lower case
    buffer_size                  : 30
    history                      : 5
    # ------------------NDT
    NDT_flag                     : False
    NDT_cfg                      : {'voxel_size': 0.5, 'overlap': True, 'min_pts_voxel': 5, 'noise': 0.05}
    NDT_out_path                 : "/home/philly12399/thesis/AB3DMOT/data/Wayside/NDT_pkl/"